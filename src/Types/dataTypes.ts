export type DataTypes = {
  id: number;
  name: string;
  region: string;
  address: string;
  cuisine?: string[];
  contact: string;
  description: string;
  coordinates?: [number, number];
  image: string;
  category: string;
};

